{
  "name": "github-as-a-db",
  "version": "1.0.0",
  "description": "Using GitHub as a database for your application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  }
}
